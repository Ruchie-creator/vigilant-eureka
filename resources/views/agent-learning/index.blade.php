<x-layouts.app heading="Agent Learning">
    <section class="rounded-lg bg-navy p-6 text-white shadow-[0_18px_50px_rgba(5,18,55,0.22)]"><p class="text-xs font-bold uppercase tracking-[.18em] text-teal-200">Outcome-informed recommendations</p><h2 class="mt-2 text-2xl font-bold">Agent Learning</h2><p class="mt-2 max-w-3xl text-sm text-slate-300">Review workspace-specific patterns derived from measured outcomes. Learning influences confidence and prioritisation, but never approves or executes an action.</p></section>
    <form method="GET" class="mt-5 grid gap-3 rounded-lg bg-white p-4 shadow-[0_0_0_1px_rgba(5,18,55,0.06)] sm:grid-cols-2 lg:grid-cols-5"><select name="website_id" class="min-h-10 rounded-lg border border-slate-200 px-3 text-sm"><option value="">All workspaces</option>@foreach($websites as $website)<option value="{{ $website->id }}" @selected(request('website_id')==$website->id)>{{ $website->name }}</option>@endforeach</select><select name="agent_id" class="min-h-10 rounded-lg border border-slate-200 px-3 text-sm"><option value="">All agents</option>@foreach($agents as $agent)<option value="{{ $agent->id }}" @selected(request('agent_id')==$agent->id)>{{ $agent->name }}</option>@endforeach</select><select name="conversion_goal" class="min-h-10 rounded-lg border border-slate-200 px-3 text-sm"><option value="">All conversion goals</option>@foreach($conversionGoals as $goal)<option value="{{ $goal }}" @selected(request('conversion_goal')===$goal)>{{ str_replace('_',' ',ucfirst($goal)) }}</option>@endforeach</select><select name="result_type" class="min-h-10 rounded-lg border border-slate-200 px-3 text-sm"><option value="">All result types</option>@foreach(['successful_action'=>'Successful','unsuccessful_action'=>'Unsuccessful','inconclusive_action'=>'Inconclusive','performance_pattern'=>'Pattern'] as $value=>$label)<option value="{{ $value }}" @selected(request('result_type')===$value)>{{ $label }}</option>@endforeach</select><select name="confidence" class="min-h-10 rounded-lg border border-slate-200 px-3 text-sm"><option value="">Any confidence</option>@foreach(['high','medium','low'] as $value)<option value="{{ $value }}" @selected(request('confidence')===$value)>{{ ucfirst($value) }}</option>@endforeach</select><div class="flex gap-2 sm:col-span-2 lg:col-span-5"><button class="min-h-10 rounded-lg bg-teal px-4 text-sm font-semibold text-white">Apply filters</button><a href="{{ route('agent-learning.index') }}" class="inline-flex min-h-10 items-center rounded-lg border border-slate-200 px-4 text-sm font-semibold">Reset</a></div></form>
    <section class="mt-6 grid gap-4 xl:grid-cols-2">@forelse($memories as $memory)<article class="rounded-lg bg-white p-5 shadow-[0_0_0_1px_rgba(5,18,55,0.06),0_14px_34px_rgba(5,18,55,0.07)]"><div class="flex flex-wrap items-start justify-between gap-3"><div><div class="flex flex-wrap gap-2"><span class="rounded-full bg-teal/10 px-2.5 py-1 text-xs font-semibold text-teal">{{ str_replace('_',' ',ucfirst($memory->memory_type)) }}</span><span class="rounded-full {{ $memory->enabled ? 'bg-emerald-50 text-emerald-700' : 'bg-slate-100 text-slate-500' }} px-2.5 py-1 text-xs font-semibold">{{ $memory->enabled ? 'Enabled' : 'Disabled' }}</span></div><h2 class="mt-3 font-bold text-navy">{{ data_get($memory->learning_metadata,'action_type','Measured action') }}</h2><p class="mt-1 text-sm text-slate-500">{{ $memory->website?->name }} &middot; {{ $memory->agent?->name }}</p></div><span class="text-sm font-bold text-navy">{{ number_format(($memory->confidence ?? 0)*100) }}%</span></div><p class="mt-4 text-sm leading-6 text-slate-600">{{ $memory->memory_value }}</p><dl class="mt-4 grid gap-3 text-xs sm:grid-cols-2"><div><dt class="font-semibold text-slate-400">Affected page/query</dt><dd class="mt-1 break-words text-slate-700">{{ data_get($memory->learning_metadata,'affected_source','Workspace goal') }}</dd></div><div><dt class="font-semibold text-slate-400">Last used</dt><dd class="mt-1 text-slate-700">{{ $memory->last_used_at?->diffForHumans() ?? 'Not used yet' }}</dd></div></dl>@if($memory->outcome && $memory->source_type==='action_outcome')<a href="{{ route('action-outcomes.show',$memory->outcome) }}" class="mt-3 inline-flex text-sm font-semibold text-teal">View original outcome</a>@endif<form method="POST" action="{{ route('agent-learning.update',$memory) }}" class="mt-4 grid gap-3 rounded-lg bg-slate-50 p-3 sm:grid-cols-3">@csrf @method('PATCH')<select name="memory_type" class="min-h-10 rounded-lg border border-slate-200 px-2 text-sm">@foreach(['successful_action'=>'Successful','unsuccessful_action'=>'Unsuccessful','inconclusive_action'=>'Inconclusive','performance_pattern'=>'Pattern'] as $value=>$label)<option value="{{ $value }}" @selected($memory->memory_type===$value)>{{ $label }}</option>@endforeach</select><input type="number" name="confidence" value="{{ $memory->confidence }}" min="0" max="1" step="0.05" class="min-h-10 rounded-lg border border-slate-200 px-3 text-sm"><select name="enabled" class="min-h-10 rounded-lg border border-slate-200 px-2 text-sm"><option value="1" @selected($memory->enabled)>Enabled</option><option value="0" @selected(!$memory->enabled)>Disabled</option></select><textarea name="review_notes" rows="2" placeholder="Review notes" class="rounded-lg border border-slate-200 p-2 text-sm sm:col-span-3">{{ $memory->review_notes }}</textarea><button class="min-h-10 rounded-lg bg-navy px-4 text-sm font-semibold text-white sm:col-span-3">Save correction</button></form></article>@empty<div class="rounded-lg bg-white px-6 py-16 text-center xl:col-span-2"><i data-lucide="brain-circuit" class="mx-auto size-9 text-slate-300"></i><p class="mt-3 font-semibold text-navy">No measured learning yet</p><p class="mt-1 text-sm text-slate-500">Evaluated action outcomes will create reviewable learning patterns here.</p></div>@endforelse</section><div class="mt-6">{{ $memories->links() }}</div>
</x-layouts.app>
