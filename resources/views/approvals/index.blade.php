<x-layouts.app heading="Approval Inbox">
    <section class="rounded-lg bg-navy p-6 text-white shadow-[0_18px_50px_rgba(5,18,55,0.22)]"><div class="flex flex-wrap items-end justify-between gap-4"><div><p class="text-xs font-bold uppercase tracking-[.18em] text-teal-200">Human review required</p><h2 class="mt-2 text-2xl font-bold">Approval Inbox</h2><p class="mt-2 max-w-2xl text-sm text-slate-300">Review agent recommendations, suggested tasks, plans, and high-priority handoffs. Approval never publishes or sends anything automatically.</p></div><a href="{{ route('agent-operations.index') }}" class="inline-flex min-h-10 items-center gap-2 rounded-lg bg-white/10 px-4 text-sm font-semibold"><i data-lucide="activity" class="size-4"></i>Operations</a></div></section>

    <form method="GET" class="mt-5 grid gap-3 rounded-lg bg-white p-4 shadow-[0_0_0_1px_rgba(5,18,55,0.06)] sm:grid-cols-2 lg:grid-cols-4 xl:grid-cols-8">
        <select name="website_id" class="min-h-10 rounded-lg border border-slate-200 bg-white px-3 text-sm text-navy"><option value="">All workspaces</option>@foreach($websites as $website)<option value="{{ $website->id }}" @selected(request('website_id') == $website->id)>{{ $website->name }}</option>@endforeach</select>
        <select name="agent_id" class="min-h-10 rounded-lg border border-slate-200 bg-white px-3 text-sm text-navy"><option value="">All agents</option>@foreach($agents as $agent)<option value="{{ $agent->id }}" @selected(request('agent_id') == $agent->id)>{{ $agent->name }}</option>@endforeach</select>
        <select name="item_type" class="min-h-10 rounded-lg border border-slate-200 bg-white px-3 text-sm text-navy"><option value="">All item types</option>@foreach(['action'=>'Agent action','task'=>'Suggested task','plan'=>'Weekly plan','handoff'=>'Handoff'] as $value=>$label)<option value="{{ $value }}" @selected(request('item_type')===$value)>{{ $label }}</option>@endforeach</select>
        <select name="priority" class="min-h-10 rounded-lg border border-slate-200 bg-white px-3 text-sm text-navy"><option value="">All priorities</option>@foreach(['critical','high','medium','low'] as $value)<option value="{{ $value }}" @selected(request('priority')===$value)>{{ ucfirst($value) }}</option>@endforeach</select>
        <select name="status" class="min-h-10 rounded-lg border border-slate-200 bg-white px-3 text-sm text-navy"><option value="">Awaiting review</option>@foreach(['pending','reviewed','draft','accepted','failed'] as $value)<option value="{{ $value }}" @selected(request('status')===$value)>{{ ucfirst($value) }}</option>@endforeach</select>
        <select name="conversion_goal" class="min-h-10 rounded-lg border border-slate-200 bg-white px-3 text-sm text-navy"><option value="">All goals</option>@foreach($conversionGoals as $goal)<option value="{{ $goal }}" @selected(request('conversion_goal')===$goal)>{{ str_replace('_',' ',ucfirst($goal)) }}</option>@endforeach</select>
        <input type="date" name="date_start" value="{{ request('date_start') }}" title="Created from" class="min-h-10 rounded-lg border border-slate-200 px-3 text-sm text-navy"><input type="date" name="date_end" value="{{ request('date_end') }}" title="Created through" class="min-h-10 rounded-lg border border-slate-200 px-3 text-sm text-navy">
        <div class="flex gap-2 sm:col-span-2 lg:col-span-4 xl:col-span-8"><button class="min-h-10 rounded-lg bg-navy px-4 text-sm font-semibold text-white">Apply filters</button><a href="{{ route('approvals.index') }}" class="inline-flex min-h-10 items-center rounded-lg border border-slate-200 px-4 text-sm font-semibold text-slate-600">Reset</a></div>
    </form>

    <section class="mt-6 grid gap-4">@forelse($items as $item)<article class="rounded-lg bg-white p-5 shadow-[0_0_0_1px_rgba(5,18,55,0.06),0_14px_34px_rgba(5,18,55,0.07)]"><div class="flex flex-wrap items-start justify-between gap-3"><div class="min-w-0"><div class="flex flex-wrap gap-2"><span class="rounded-full bg-teal/10 px-2.5 py-1 text-xs font-semibold text-teal">{{ str_replace('_',' ',ucfirst($item['action_type'])) }}</span><span class="rounded-full px-2.5 py-1 text-xs font-semibold {{ in_array($item['priority'],['critical','high']) ? 'bg-rose-50 text-rose-700' : 'bg-amber-50 text-amber-700' }}">{{ ucfirst($item['priority']) }}</span><span class="rounded-full bg-slate-100 px-2.5 py-1 text-xs font-semibold text-slate-600">{{ ucfirst($item['status']) }}</span></div><h2 class="mt-3 text-lg font-bold text-navy">{{ $item['title'] }}</h2><p class="mt-1 text-sm font-medium text-slate-600">{{ $item['website']->name }}</p><p class="mt-1 text-xs text-slate-500">{{ $item['agent']?->name ?? 'Marketing team' }} &middot; {{ $item['created_at']->format('M j, Y g:i A') }}</p></div><span class="inline-flex items-center gap-2 rounded-lg bg-amber-50 px-3 py-2 text-xs font-semibold text-amber-800"><i data-lucide="shield-alert" class="size-4"></i>Approval required</span></div><dl class="mt-5 grid gap-4 md:grid-cols-2"><div><dt class="text-xs font-bold uppercase text-slate-400">Affected page, query, or audience</dt><dd class="mt-1 break-words text-sm text-slate-700">{{ $item['affected'] ?: 'Configured workspace audience' }}</dd></div><div><dt class="text-xs font-bold uppercase text-slate-400">Recommendation</dt><dd class="mt-1 text-sm font-medium leading-6 text-navy">{{ $item['recommendation'] ?: 'Review the attached operational item.' }}</dd></div><div><dt class="text-xs font-bold uppercase text-slate-400">Expected result</dt><dd class="mt-1 text-sm leading-6 text-slate-700">{{ $item['expected_result'] ?: 'Progress toward the configured conversion goal.' }}</dd></div><div><dt class="text-xs font-bold uppercase text-slate-400">Conversion goal</dt><dd class="mt-1 text-sm text-slate-700">{{ str_replace('_',' ',ucfirst($item['conversion_goal'])) }}</dd></div></dl><details class="mt-4 rounded-lg bg-slate-50 p-4"><summary class="text-sm font-semibold text-navy">Supporting evidence</summary><pre class="mt-3 whitespace-pre-wrap break-words font-sans text-xs leading-5 text-slate-600">{{ is_array($item['evidence']) ? json_encode($item['evidence'], JSON_PRETTY_PRINT|JSON_UNESCAPED_SLASHES) : ($item['evidence'] ?: 'No additional evidence supplied.') }}</pre></details><form method="POST" action="{{ route('approvals.update', [$item['type'], $item['id']]) }}" class="mt-4">@csrf @method('PATCH')<textarea name="review_notes" rows="2" placeholder="Optional review notes" class="w-full rounded-lg border border-slate-200 px-3 py-2 text-sm text-navy"></textarea><details class="mt-2 rounded-lg border border-slate-200 p-3"><summary class="text-xs font-semibold text-slate-600">Revision reason</summary><textarea name="revision_reason" rows="2" placeholder="Describe exactly what the agent should revise" class="mt-2 w-full rounded-lg border border-slate-200 px-3 py-2 text-sm text-navy"></textarea></details><div class="mt-3 flex flex-wrap gap-2"><button name="operation" value="approve" class="min-h-10 rounded-lg bg-teal px-4 text-sm font-semibold text-white">Approve</button><button name="operation" value="approve_task" class="min-h-10 rounded-lg bg-navy px-4 text-sm font-semibold text-white">Approve &amp; Create Task</button><button name="operation" value="request_revision" class="min-h-10 rounded-lg border border-amber-300 px-4 text-sm font-semibold text-amber-800">Request Revision</button><button name="operation" value="ignore" class="min-h-10 rounded-lg border border-slate-200 px-4 text-sm font-semibold text-slate-600">Ignore</button></div></form></article>@empty<div class="rounded-lg bg-white px-6 py-16 text-center shadow-[0_0_0_1px_rgba(5,18,55,0.06)]"><i data-lucide="inbox" class="mx-auto size-9 text-slate-300"></i><p class="mt-3 font-semibold text-navy">Approval inbox is clear</p><p class="mt-1 text-sm text-slate-500">New agent recommendations and draft plans will appear here.</p></div>@endforelse</section><div class="mt-6">{{ $items->links() }}</div>
</x-layouts.app>
