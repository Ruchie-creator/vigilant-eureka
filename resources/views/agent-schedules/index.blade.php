<x-layouts.app heading="Agent Schedules">
    <section class="rounded-lg bg-navy p-6 text-white shadow-[0_18px_50px_rgba(5,18,55,0.22)]"><div class="flex flex-wrap items-end justify-between gap-4"><div><p class="text-xs font-bold uppercase tracking-[.18em] text-teal-200">Scheduled operations</p><h2 class="mt-2 text-2xl font-bold">Agent run schedule</h2><p class="mt-2 max-w-2xl text-sm text-slate-300">Review recurring analysis and planning runs. Generated actions remain pending until a person approves them.</p></div><span class="rounded-lg bg-white/10 px-3 py-2 text-xs font-semibold">Shared-hosting compatible</span></div></section>
    <div class="mt-6 flex flex-wrap gap-3 rounded-lg bg-white p-4 shadow-[0_0_0_1px_rgba(5,18,55,0.06)]"><form method="GET" class="flex flex-wrap gap-3"><select name="website_id" class="min-h-10 min-w-60 rounded-lg border border-slate-200 bg-white px-3 text-sm text-navy"><option value="">All workspaces</option>@foreach($websites as $website)<option value="{{ $website->id }}" @selected(request('website_id') == $website->id)>{{ $website->name }}</option>@endforeach</select><button class="min-h-10 rounded-lg bg-navy px-4 text-sm font-semibold text-white">Filter</button></form>@if(request('website_id'))<form method="POST" action="{{ route('websites.agent-schedules.defaults', request('website_id')) }}">@csrf<button class="min-h-10 rounded-lg bg-teal px-4 text-sm font-semibold text-white">Create missing defaults</button></form>@endif</div>
    <section class="mt-6 grid gap-4">@forelse($schedules as $schedule)<article class="rounded-lg bg-white p-5 shadow-[0_0_0_1px_rgba(5,18,55,0.06),0_12px_30px_rgba(5,18,55,0.06)]"><div class="flex flex-wrap items-start justify-between gap-4"><div><div class="flex flex-wrap items-center gap-2"><span class="rounded-full bg-teal/10 px-2.5 py-1 text-xs font-semibold text-teal">{{ str_replace('_', ' ', ucfirst($schedule->schedule_type)) }}</span><span class="rounded-full px-2.5 py-1 text-xs font-semibold {{ $schedule->enabled ? 'bg-emerald-50 text-emerald-700' : 'bg-slate-100 text-slate-500' }}">{{ $schedule->enabled ? 'Enabled' : 'Disabled' }}</span></div><h2 class="mt-3 font-bold text-navy">{{ $schedule->website->name }}</h2><p class="mt-1 text-sm text-slate-500">{{ $schedule->agent?->name ?? 'Full marketing team' }} &middot; {{ ucfirst($schedule->frequency) }}</p><p class="mt-2 text-xs text-slate-500">Next: {{ $schedule->next_run_at?->format('M j, Y g:i A') ?? 'Event or manual' }} &middot; Last: {{ $schedule->last_run_at?->format('M j, Y g:i A') ?? 'Never' }} &middot; Status: {{ ucfirst($schedule->last_status ?? 'not run') }}</p></div><div class="flex flex-wrap gap-2"><form method="POST" action="{{ route('agent-schedules.toggle', $schedule) }}">@csrf @method('PATCH')<button class="min-h-9 rounded-lg border border-slate-200 px-3 text-xs font-semibold text-slate-700">{{ $schedule->enabled ? 'Disable' : 'Enable' }}</button></form>@unless($schedule->schedule_type === 'after_gsc_sync')<form method="POST" action="{{ route('agent-schedules.run', $schedule) }}">@csrf<button class="min-h-9 rounded-lg bg-teal px-3 text-xs font-semibold text-white">Run now</button></form>@endunless<a href="{{ route('websites.weekly-marketing-plans.index', $schedule->website) }}" class="inline-flex min-h-9 items-center rounded-lg bg-navy px-3 text-xs font-semibold text-white">Plans</a></div></div><form method="POST" action="{{ route('agent-schedules.update', $schedule) }}" class="mt-4 grid gap-3 border-t border-slate-100 pt-4 sm:grid-cols-2 lg:grid-cols-5">@csrf @method('PATCH')<select name="frequency" class="min-h-10 rounded-lg border border-slate-200 bg-white px-3 text-sm text-navy">@foreach(['daily','weekly','monthly','manual','event_triggered'] as $frequency)<option value="{{ $frequency }}" @selected($schedule->frequency === $frequency)>{{ ucfirst(str_replace('_',' ',$frequency)) }}</option>@endforeach</select><input name="run_at" type="time" value="{{ $schedule->run_at ? substr($schedule->run_at,0,5) : '' }}" class="min-h-10 rounded-lg border border-slate-200 px-3 text-sm text-navy"><input name="timezone" value="{{ $schedule->timezone }}" placeholder="Timezone" class="min-h-10 rounded-lg border border-slate-200 px-3 text-sm text-navy"><select name="weekday" class="min-h-10 rounded-lg border border-slate-200 bg-white px-3 text-sm text-navy">@foreach(['Sunday','Monday','Tuesday','Wednesday','Thursday','Friday','Saturday'] as $day => $label)<option value="{{ $day }}" @selected(data_get($schedule->settings,'weekday',1) == $day)>{{ $label }}</option>@endforeach</select><button class="min-h-10 rounded-lg bg-navy px-4 text-sm font-semibold text-white">Save schedule</button></form></article>@empty<div class="rounded-lg bg-white px-6 py-14 text-center"><i data-lucide="calendar-x" class="mx-auto size-8 text-slate-300"></i><p class="mt-3 font-semibold text-navy">No schedules configured</p><p class="mt-1 text-sm text-slate-500">Choose a workspace above and create its missing defaults.</p></div>@endforelse</section><div class="mt-6">{{ $schedules->links() }}</div>
</x-layouts.app>
