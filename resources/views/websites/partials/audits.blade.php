<section class="rounded-lg border border-slate-200 bg-white shadow-soft">
    <div class="border-b border-slate-100 px-5 py-4"><h2 class="font-semibold text-navy">Recent Audits</h2></div>
    <div class="divide-y divide-slate-100">@forelse($audits as $audit)<div class="px-5 py-4"><div class="flex justify-between gap-4"><p class="font-semibold">{{ $audit->page_title ?: 'Untitled page' }}</p><span class="rounded-full px-3 py-1 text-xs font-semibold {{ $audit->is_indexable ? 'bg-emerald-50 text-emerald-700' : 'bg-amber-50 text-amber-700' }}">{{ $audit->is_indexable ? 'Indexable' : 'Review' }}</span></div><p class="mt-1 text-sm text-slate-500">HTTP {{ $audit->http_status ?: 'n/a' }} · {{ $audit->created_at->diffForHumans() }}</p>@if($audit->missing_fields)<p class="mt-2 text-sm text-slate-600">Missing: {{ implode(', ', $audit->missing_fields) }}</p>@endif</div>@empty<div class="px-5 py-10 text-center text-sm text-slate-500">No audits yet.</div>@endforelse</div>
</section>
