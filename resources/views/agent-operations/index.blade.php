<x-layouts.app heading="Agent Operations">
    <section class="rounded-lg bg-navy p-6 text-white shadow-[0_18px_50px_rgba(5,18,55,0.22)]"><div class="flex flex-wrap items-end justify-between gap-4"><div><p class="text-xs font-bold uppercase tracking-[.18em] text-teal-200">Operational visibility</p><h2 class="mt-2 text-2xl font-bold">Agent Operations</h2><p class="mt-2 max-w-2xl text-sm text-slate-300">Monitor runs, schedules, handoffs, and review activity without exposing credentials or executing external changes.</p></div><a href="{{ route('approvals.index') }}" class="inline-flex min-h-10 items-center gap-2 rounded-lg bg-teal px-4 text-sm font-semibold"><i data-lucide="inbox" class="size-4"></i>Open approvals</a></div></section>
    @php($cards = [['Active runs',$summary['active_runs'],'loader-circle'],['Completed today',$summary['completed_today'],'circle-check'],['Failed runs',$summary['failed_runs'],'circle-alert'],['Pending approvals',$summary['pending_approvals'],'inbox'],['Pending handoffs',$summary['pending_handoffs'],'git-pull-request-arrow'],['Due schedules',$summary['due_schedules'],'calendar-clock'],['Plans awaiting approval',$summary['pending_plans'],'clipboard-list'],['Agent-created tasks',$summary['agent_tasks'],'list-checks']])
    <section class="mt-5 grid gap-3 sm:grid-cols-2 lg:grid-cols-4">@foreach($cards as [$label,$value,$icon])<article class="rounded-lg bg-white p-4 shadow-[0_0_0_1px_rgba(5,18,55,0.06),0_10px_24px_rgba(5,18,55,0.06)]"><div class="flex items-center justify-between"><p class="text-xs font-semibold text-slate-500">{{ $label }}</p><i data-lucide="{{ $icon }}" class="size-4 text-teal"></i></div><p class="mt-2 text-2xl font-bold text-navy">{{ number_format($value) }}</p></article>@endforeach</section>
    <section class="mt-6 overflow-hidden rounded-lg bg-white shadow-[0_0_0_1px_rgba(5,18,55,0.06),0_14px_34px_rgba(5,18,55,0.07)]"><div class="border-b border-slate-100 p-5"><h2 class="font-bold text-navy">Recent agent runs</h2><p class="mt-1 text-sm text-slate-500">Latest manual, scheduled, event-triggered, and orchestration activity.</p></div><div class="overflow-x-auto"><table class="app-table min-w-[1050px] w-full text-left text-sm"><thead class="bg-slate-50"><tr>@foreach(['Workspace','Agent','Trigger','Status','Started','Duration','Actions','Correlation ID','Retries',''] as $heading)<th class="px-4 py-3">{{ $heading }}</th>@endforeach</tr></thead><tbody class="divide-y divide-slate-100">@forelse($runs as $run)<tr><td class="px-4 py-3 font-semibold text-navy">{{ $run->website?->name ?? 'Global' }}</td><td class="px-4 py-3">{{ $run->agent->name }}</td><td class="px-4 py-3">{{ str_replace('_',' ',ucfirst($run->trigger_type)) }}</td><td class="px-4 py-3"><span class="rounded-full bg-slate-100 px-2.5 py-1 text-xs font-semibold">{{ ucfirst($run->status) }}</span></td><td class="px-4 py-3">{{ $run->started_at?->format('M j, g:i A') ?? 'Not started' }}</td><td class="px-4 py-3">{{ $run->duration_ms !== null ? number_format($run->duration_ms).' ms' : '--' }}</td><td class="px-4 py-3">{{ $run->actions_count }}</td><td class="max-w-44 truncate px-4 py-3 font-mono text-xs" title="{{ $run->correlation_id }}">{{ $run->correlation_id ?: '--' }}</td><td class="px-4 py-3">{{ $run->retry_count }}</td><td class="px-4 py-3"><a href="{{ route('agent-operations.runs.show',$run) }}" class="inline-flex min-h-9 items-center rounded-lg bg-navy px-3 text-xs font-semibold text-white">View</a></td></tr>@empty<tr><td colspan="10" class="px-5 py-12 text-center text-slate-500">No agent runs recorded yet.</td></tr>@endforelse</tbody></table></div></section>
    <section class="mt-6 grid gap-5 xl:grid-cols-2"><div class="rounded-lg bg-white p-5 shadow-[0_0_0_1px_rgba(5,18,55,0.06)]"><h2 class="font-bold text-navy">Upcoming scheduled runs</h2><div class="mt-4 grid gap-3">@forelse($upcomingSchedules as $schedule)<div class="rounded-lg bg-slate-50 p-3"><p class="text-sm font-semibold text-navy">{{ $schedule->website->name }} &middot; {{ $schedule->agent?->name ?? 'Full team' }}</p><p class="mt-1 text-xs text-slate-500">{{ str_replace('_',' ',ucfirst($schedule->schedule_type)) }} &middot; {{ $schedule->next_run_at->format('M j, Y g:i A') }}</p></div>@empty<p class="text-sm text-slate-500">No upcoming scheduled runs.</p>@endforelse</div></div><div class="rounded-lg bg-white p-5 shadow-[0_0_0_1px_rgba(5,18,55,0.06)]"><h2 class="font-bold text-navy">Recent event-triggered runs</h2><div class="mt-4 grid gap-3">@forelse($eventRuns as $run)<a href="{{ route('agent-operations.runs.show',$run) }}" class="rounded-lg bg-slate-50 p-3"><p class="text-sm font-semibold text-navy">{{ $run->website?->name }} &middot; {{ $run->agent->name }}</p><p class="mt-1 text-xs text-slate-500">{{ data_get($run->metadata,'trigger_reason','Search Console event') }} &middot; {{ ucfirst($run->status) }}</p></a>@empty<p class="text-sm text-slate-500">No recent event-triggered runs.</p>@endforelse</div></div><div class="rounded-lg bg-white p-5 shadow-[0_0_0_1px_rgba(5,18,55,0.06)]"><h2 class="font-bold text-navy">Recent failures</h2><div class="mt-4 grid gap-3">@forelse($failures as $run)<a href="{{ route('agent-operations.runs.show',$run) }}" class="rounded-lg bg-rose-50 p-3"><p class="text-sm font-semibold text-rose-900">{{ $run->website?->name }} &middot; {{ $run->agent->name }}</p><p class="mt-1 text-xs text-rose-700">{{ $run->created_at->diffForHumans() }}</p></a>@empty<p class="text-sm text-slate-500">No recent failures.</p>@endforelse</div></div><div class="rounded-lg bg-white p-5 shadow-[0_0_0_1px_rgba(5,18,55,0.06)]"><h2 class="font-bold text-navy">Unresolved handoffs</h2><div class="mt-4 grid gap-3">@forelse($handoffs as $handoff)<div class="rounded-lg bg-slate-50 p-3"><p class="text-sm font-semibold text-navy">{{ $handoff->fromAgent->name }} to {{ $handoff->toAgent->name }}</p><p class="mt-1 text-xs text-slate-500">{{ $handoff->website->name }} &middot; {{ ucfirst($handoff->status) }}</p></div>@empty<p class="text-sm text-slate-500">No unresolved handoffs.</p>@endforelse</div></div><div class="rounded-lg bg-white p-5 shadow-[0_0_0_1px_rgba(5,18,55,0.06)] xl:col-span-2"><h2 class="font-bold text-navy">Recent approvals</h2><div class="mt-4 grid gap-3 sm:grid-cols-2">@forelse($approvals as $action)<div class="rounded-lg bg-slate-50 p-3"><p class="text-sm font-semibold text-navy">{{ $action->title }}</p><p class="mt-1 text-xs text-slate-500">{{ $action->reviewer?->name ?? 'Reviewer' }} &middot; {{ ucfirst($action->status) }} &middot; {{ $action->reviewed_at?->diffForHumans() }}</p></div>@empty<p class="text-sm text-slate-500">No approvals recorded yet.</p>@endforelse</div></div></section>
</x-layouts.app>
